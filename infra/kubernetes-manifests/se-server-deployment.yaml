apiVersion: apps/v1
kind: Deployment
metadata:
  name: se-server
  labels:
    app: se-server
spec:
  selector:
    matchLabels:
      app: se-server
  replicas: 2
  minReadySeconds: 15
  strategy:
    type: RollingUpdate
    rollingUpdate: 
      maxUnavailable: 1
      maxSurge: 1 
  template:
    metadata:
      labels:
        app: se-server
    spec:
      containers:
      - image: gcr.io/cmu14848-435323/oooscarrrr/sentiment-analysis-frontend # To be changed
        imagePullPolicy: Always
        name: se-server
        ports:
          - containerPort: 5001
